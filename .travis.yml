language: node_js
node_js:
  - "stable"
cache:
  directories:
    - "node_modules"
services:
  - postgresql

before_script:
  - npm i -g sequelize-cli
  - psql -c "create database haven;" -U postgres
